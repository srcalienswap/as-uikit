import { StepCompleted } from './StepCompleted';

describe('@asuikit/core/StepCompleted', () => {
  it('has correct displayName', () => {
    expect(StepCompleted.displayName).toStrictEqual('@asuikit/core/StepCompleted');
  });
});
